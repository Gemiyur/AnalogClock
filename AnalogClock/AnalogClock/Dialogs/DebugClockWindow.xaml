<Window x:Class="AnalogClock.Dialogs.DebugClockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnalogClock.Dialogs"
        mc:Ignorable="d"
        Title="Свойства часов"
        Width="200" Height="200" ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Closed="Window_Closed">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
                <Setter Property="Margin" Value="0,10,0,0" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <StackPanel>
            <CheckBox x:Name="IsRunningCheckBox" Content="Часы включены"
                      Style="{StaticResource CheckBoxStyle}" Click="IsRunningCheckBox_Click" />
            <CheckBox x:Name="IsTransparentCheckBox" Content="Прозрачный циферблат"
                      Style="{StaticResource CheckBoxStyle}" Click="IsTransparentCheckBox_Click" />
            <CheckBox x:Name="IsDigitsShownCheckBox" Content="Цифры на циферблате"
                      Style="{StaticResource CheckBoxStyle}" Click="IsDigitsShownCheckBox_Click" />
            <CheckBox x:Name="IsRomanDigitsCheckBox" Content="Римские цифры"
                      Style="{StaticResource CheckBoxStyle}" Click="IsRomanDigitsCheckBox_Click" />
            <Button x:Name="DigitsColorButton" Content="Цвет цифр"
                    Width="80" Height="25" HorizontalAlignment="Left"
                    Margin="0,10,0,0" Click="DigitsColorButton_Click" />
        </StackPanel>
    </Grid>
</Window>
